<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Melodix">

        <activity
            android:name=".MainActivity"
            android:exported="false">

        </activity>

        <activity
            android:name=".View.auth.LoginActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="melodix" android:host="callback" />
            </intent-filter>
        </activity>
        <activity android:name=".View.AlbumDetailActivity" android:exported="false" />
        <activity android:name=".View.ArtistDetailActivity" android:exported="false" />
        <activity android:name=".View.ArtistSongsActivity" android:exported="false" />
        <activity android:name=".View.ArtistAlbumsActivity" android:exported="false" />


        <activity android:name=".View.auth.RegisterActivity" />

        <activity android:name=".AdminActivity" />"
    </application>

</manifest>